// Monster.cpp
#include "Monster.h"

// 몬스터 클래스의 생성자 구현
Monster::Monster(int level) : level(level), name(""), health(0), maxHealth(0), attack(0), attackSpeed(0), baseExp(0), baseGold(0)
{
    // 생성자 내부에서는 특별히 할 작업이 없음
}